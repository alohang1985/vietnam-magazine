# Frontend (Next.js) — Vietnam Travel Magazine

Run locally:

$ cd apps/frontend-next
$ npm install
$ NEXT_PUBLIC_CMS_URL=http://localhost:1337 npm run dev

Notes:
- Uses Tailwind CSS, Google fonts for Noto Serif KR and Inter
- Expects Strapi API at NEXT_PUBLIC_CMS_URL
