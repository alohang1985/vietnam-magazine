{
  "dependencies": {
    "puppeteer": "^21.3.8"
  },
  "scripts": {
    "start": "cd apps/strapi-cms && npm run start",
    "build": "cd apps/strapi-cms && npm run build",
    "postinstall": "cd apps/strapi-cms && npm install --no-audit --no-fund"
  }
}